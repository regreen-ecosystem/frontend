import React from 'react';
import { ColumnDetails } from './types/CustomTableProps';

const FilterPopup: React.FC<{ columns: ColumnDetails[] }> = ({ columns }) => {
  return <></>;
};

export default FilterPopup;
